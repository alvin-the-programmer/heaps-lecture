# Max Heap Visualizer in React

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Usage:

Install dependencies:

```
npm install
```

Start server:

```
npm start
```

Navigate to `http://localhost:3000/`
